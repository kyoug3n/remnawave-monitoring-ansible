---
# ↓ override in hosts.yml → monitoring.vars if needed ↓
prometheus_port: 9090
grafana_port: 3000
whitebox_port: 9116
blackbox_port: 9115
# ↑ override in hosts.yml → monitoring.vars if needed ↑

# ↓ do not change ↓
monitoring_base_path: "/opt/monitoring"
monitoring_network_name: "monitoring-network"
# ↑ do not change ↑
